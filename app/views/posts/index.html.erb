<h1> Posts </h1>
<% @posts.each do |post| %>
    <h2><%= link_to post.title, post_path(post.id) %></h2>
    <h3><%= post.author %></h2>
    <p><%= post.body %></p>
<% end %>

<p><%= link_to 'New Post', new_post_path %></p>
